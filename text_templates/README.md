# UI templates

The auto player expects the following image files in this folder:

- `bid.png`
- `force_open.png`
- `play_again.png`
- `start.png`

These images are interface-specific reference crops used by OpenCV template matching. They were not present in the recovered archived project files, so they are intentionally not fabricated here.
